# =============================================================================
# doc-pipeline - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Hugging Face (REQUIRED for model downloads)
# -----------------------------------------------------------------------------
# Get your token at: https://huggingface.co/settings/tokens
# Required for: faster downloads, higher rate limits, gated models (PyAnnote)
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# Classifier Settings
# -----------------------------------------------------------------------------
# DOC_PIPELINE_CLASSIFIER_MODEL_PATH=models/classifier.pth
# DOC_PIPELINE_CLASSIFIER_MODEL_TYPE=efficientnet_b0  # efficientnet_b0, b2, b4
# DOC_PIPELINE_CLASSIFIER_DEVICE=cuda:0              # cuda:0, cuda:1, cpu
# DOC_PIPELINE_CLASSIFIER_FP8=false                  # Requires Hopper/Blackwell GPU

# -----------------------------------------------------------------------------
# Extractor Settings (VLM)
# -----------------------------------------------------------------------------
# DOC_PIPELINE_EXTRACTOR_BACKEND=qwen-vl             # qwen-vl (~16GB) or easy-ocr (~2GB)
# DOC_PIPELINE_EXTRACTOR_DEVICE=cuda:0               # Can be different from classifier
# DOC_PIPELINE_EXTRACTOR_MODEL_QWEN=Qwen/Qwen2.5-VL-7B-Instruct

# -----------------------------------------------------------------------------
# API Settings
# -----------------------------------------------------------------------------
# DOC_PIPELINE_API_HOST=0.0.0.0
# DOC_PIPELINE_API_PORT=8001

# API Key Authentication (optional but recommended for public exposure)
# If set, all endpoints except /health require X-API-Key header
# Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# DOC_PIPELINE_API_KEY=your-secret-api-key

# -----------------------------------------------------------------------------
# Ngrok Settings (for start-server.sh)
# -----------------------------------------------------------------------------
# Custom domain for paid ngrok accounts
# Can also be passed as argument: ./start-server.sh start meu-dominio.ngrok.io
# NGROK_DOMAIN=meu-dominio.ngrok.io

# -----------------------------------------------------------------------------
# General Settings
# -----------------------------------------------------------------------------
# DOC_PIPELINE_MIN_CONFIDENCE=0.5                    # Minimum confidence threshold
# DOC_PIPELINE_WARMUP_ON_START=true                  # Load models at startup
