# Lightweight dependencies for stateless workers (no GPU, no PyTorch)
# Workers delegate all GPU work to the centralized inference server.

# Config & validation
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# API (health check endpoint)
fastapi>=0.109.0
uvicorn>=0.27.0

# Logging & observability
structlog>=24.0.0
prometheus-client>=0.20.0
sentry-sdk[fastapi]>=2.0.0

# Image (only for file existence checks, not processing)
pillow>=10.0.0
pillow-heif>=0.16.0

# Redis queue
redis[hiredis]>=5.0.0

# HTTP client (webhook delivery)
httpx>=0.27.0

# OCR worker health check server
aiohttp>=3.9.0
