# doc-classifier como dependência editável
-e ../doc-classifier

# Core
torch>=2.0.0
torchvision>=0.15.0

# VLM backends
transformers>=4.49.0
accelerate>=0.26.0
qwen-vl-utils>=0.0.8

# Config & validation
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# API
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.9

# Observability
structlog>=24.0.0
prometheus-client>=0.20.0

# Database (auth)
asyncpg>=0.29.0

# Image processing
pillow>=10.0.0
pillow-heif>=0.16.0  # Suporte a HEIC/HEIF (iPhone) e AVIF

# Redis queue
redis[hiredis]>=5.0.0

# HTTP client (webhook delivery)
httpx>=0.27.0
slowapi>=0.1.9
