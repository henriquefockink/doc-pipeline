# OCR Worker dependencies
# Lighter than main worker - no transformers/torch (PaddleOCR uses PaddlePaddle)

# PaddleOCR and dependencies (CPU version - PaddleOCR is fast enough without GPU)
paddlepaddle>=2.6.0,<3.0  # CPU version (GPU version requires CUDA image)
paddleocr>=2.7.0,<3.0  # Use 2.x API (3.x has breaking changes)

# PDF handling
PyMuPDF>=1.24.0

# Image processing
Pillow>=10.0.0
pillow-heif>=0.15.0

# API and async
aiohttp>=3.9.0
redis>=5.0.0
fastapi>=0.115.0
starlette>=0.40.0

# Config and logging
pydantic>=2.0.0
pydantic-settings>=2.0.0
structlog>=24.0.0

# Metrics
prometheus-client>=0.19.0
